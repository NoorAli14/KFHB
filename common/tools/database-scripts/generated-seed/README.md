This folder is used to output the generated seed files.

For regeneration of the seed files,
* Delete all .sql files in this folder.
* Do not delete this folder.
* Run the package as `npm run generate:seed:all`
