This folder is used to output the generated schema files.

For regeneration of the schema files,
* Delete all .sql files in this folder.
* Do not delete this folder.
* Run the package as `npm run generate:schema:all`
